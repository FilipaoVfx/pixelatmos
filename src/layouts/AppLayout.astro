---
import { AD_LOADER_SRC } from '../config/adsense';
import Sidebar from '../components/layout/Sidebar.astro';
import { AppTopBar } from '../components/layout/AppTopBar';
import ConsentBanner from '../components/ConsentBanner.astro';
import '../index.css';

interface Props {
  title: string;
  description?: string;
  image?: string;
  noindex?: boolean;
}

const {
  title,
  description = 'Creative tools for analyzing visuals, generating ideas and building experiences. Free, browser-based.',
  image = '/og-image.webp',
  noindex = false,
} = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const siteURL = Astro.site?.toString().replace(/\/$/, '') ?? 'https://pixelatmos.com';

// Google Search Console verification. Set PUBLIC_GOOGLE_SITE_VERIFICATION in the
// Cloudflare env to render the meta tag (leave unset to omit it).
const googleSiteVerification = import.meta.env.PUBLIC_GOOGLE_SITE_VERIFICATION;

// Site-wide structured data (Organization + WebSite) for SEO / E-E-A-T signals.
const structuredData = {
  '@context': 'https://schema.org',
  '@graph': [
    {
      '@type': 'Organization',
      '@id': `${siteURL}/#organization`,
      name: 'Pixelatmos',
      alternateName: 'Pixelatmos',
      url: `${siteURL}/`,
      logo: {
        '@type': 'ImageObject',
        url: `${siteURL}/og-image.webp`,
      },
      description:
        'Pixelatmos is an independent creative-tools studio and curated gallery for tech-culture visuals — browser-based tools for SVG, 3D and image work (3D Lab, SVG Optimizer, 3D Text Generator, ASCII Lab, Visual Protocol) plus original cyberpunk and developer-culture wallpapers.',
      foundingDate: '2024',
      // Entity linking: every profile that represents this brand elsewhere.
      // Add new ones as they go live — an unresolvable sameAs is worse than a
      // missing one, so nothing goes in here before it exists.
      sameAs: ['https://github.com/FilipaoVfx'],
      founder: { '@id': `${siteURL}/#person` },
      knowsAbout: ['SVG', '3D graphics', 'glTF', 'WebGL', 'Generative art', 'Graphic design', 'ASCII art'],
    },
    {
      '@type': 'Person',
      '@id': `${siteURL}/#person`,
      name: 'FilipaoVfx',
      url: `${siteURL}/about/`,
      image: `${siteURL}/filipaovfx-avatar.png`,
      description:
        'Independent designer and developer building browser-based creative tools for SVG, 3D and visual work.',
      sameAs: ['https://github.com/FilipaoVfx'],
      knowsAbout: ['SVG', '3D graphics', 'WebGL', 'Creative coding', 'Visual design'],
      worksFor: { '@id': `${siteURL}/#organization` },
    },
    {
      '@type': 'WebSite',
      '@id': `${siteURL}/#website`,
      url: `${siteURL}/`,
      name: 'Pixelatmos',
      description:
        'Free, browser-based creative tools and a curated gallery of tech-culture wallpapers.',
      inLanguage: 'en',
      publisher: { '@id': `${siteURL}/#organization` },
    },
  ],
};
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content={description} />
    {noindex && <meta name="robots" content="noindex, follow" />}
    {googleSiteVerification && <meta name="google-site-verification" content={googleSiteVerification} />}
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <!-- Google Consent Mode v2 — MUST run before AdSense. Everything denied by
         default until the user chooses (EEA/UK-safe); a stored choice is applied
         immediately. Banner UI is in <ConsentBanner />. -->
    <script is:inline>
      window.dataLayer = window.dataLayer || [];
      function gtag(){ dataLayer.push(arguments); }
      gtag('consent', 'default', {
        ad_storage: 'denied',
        ad_user_data: 'denied',
        ad_personalization: 'denied',
        analytics_storage: 'denied',
        wait_for_update: 500,
      });
      try {
        if (localStorage.getItem('cw-consent') === 'granted') {
          gtag('consent', 'update', {
            ad_storage: 'granted',
            ad_user_data: 'granted',
            ad_personalization: 'granted',
            analytics_storage: 'granted',
          });
        }
      } catch (e) {}
    </script>

    <script async src={AD_LOADER_SRC} crossorigin="anonymous"></script>

    <!-- Microsoft Clarity — loaded ONLY after analytics consent (see ConsentBanner).
         cwLoadClarity() is idempotent; called on load if consent is stored, or by
         the banner on "Accept". Not loaded on reject/undecided. -->
    <script is:inline>
      (function () {
        var loaded = false;
        window.cwLoadClarity = function () {
          if (loaded) return;
          loaded = true;
          (function (c, l, a, r, i, t, y) {
            c[a] = c[a] || function () { (c[a].q = c[a].q || []).push(arguments) };
            t = l.createElement(r); t.async = 1; t.src = "https://www.clarity.ms/tag/" + i;
            y = l.getElementsByTagName(r)[0]; y.parentNode.insertBefore(t, y);
          })(window, document, "clarity", "script", "xfwk39u366");
        };
        try {
          if (localStorage.getItem('cw-consent') === 'granted') window.cwLoadClarity();
        } catch (e) {}
      })();
    </script>
    <link rel="icon" type="image/x-icon" href="/iconCulture.ico" />
    <meta name="generator" content={Astro.generator} />
    <link rel="canonical" href={canonicalURL} />

    <!-- Site-wide structured data (Organization + WebSite) -->
    <script type="application/ld+json" set:html={JSON.stringify(structuredData)} />

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={Astro.url} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={new URL(image, Astro.url)} />
    <meta property="twitter:card" content="summary_large_image" />

    <title>{title}</title>
  </head>
  <body>
    <Sidebar />

    <div class="app-content ml-[260px] min-h-screen transition-[margin] duration-200">
      <AppTopBar client:load />
      <main class="px-6 py-8 md:px-10">
        <slot />
      </main>

      <footer class="border-t-2 border-white/10 px-6 py-10 text-center md:px-10">
        <div class="mb-6 flex flex-wrap justify-center gap-x-6 gap-y-2 font-brutal text-xs font-black uppercase tracking-wider">
          <a href="/blog/" class="text-gray-400 transition-colors hover:text-brutal-neon-cyan">Blog</a>
          <a href="/about/" class="text-gray-400 transition-colors hover:text-brutal-neon-cyan">About</a>
          <a href="/contact/" class="text-gray-400 transition-colors hover:text-brutal-neon-yellow">Contact</a>
          <a href="/license" class="text-gray-400 transition-colors hover:text-brutal-neon-green">License</a>
          <a href="/privacy-policy" class="text-gray-400 transition-colors hover:text-brutal-neon-green">Privacy</a>
          <a href="/terms" class="text-gray-400 transition-colors hover:text-brutal-neon-green">Terms</a>
          <button type="button" onclick="window.cwOpenConsent&&window.cwOpenConsent()" class="font-brutal text-xs font-black uppercase tracking-wider text-gray-400 transition-colors hover:text-brutal-neon-cyan">Cookie preferences</button>
        </div>
        <div class="mx-auto mb-4 h-1 w-20 bg-brutal-neon-cyan"></div>
        <p class="font-mono text-xs text-gray-500">
          &copy; {new Date().getFullYear()} <span class="text-white">Pixelatmos</span>. Protocol initialized.
        </p>
      </footer>
    </div>

    <ConsentBanner />
  </body>
</html>

<style is:global>
  body {
    margin: 0;
    padding: 0;
    min-height: 100vh;
    background: #0a0a0f;
    color: #f0f0f0;
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  }
</style>
